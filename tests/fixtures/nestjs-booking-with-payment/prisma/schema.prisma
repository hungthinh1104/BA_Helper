generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Booking {
  id     String @id
  status String
}

model PaymentTransaction {
  id        String @id
  bookingId String
  status    String
}
