# NestJS Order Inventory Fixture

This is a synthetic fixture designed to test the RAG engine against a completely different domain: Order and Inventory management.

It contains deliberate noise files (`discount.service.ts`, `notification.service.ts`) to test retrieval precision.
